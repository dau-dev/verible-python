[build-system]
requires = [
    "hatchling",
]
build-backend = "hatchling.build"

[project]
name = "verible"
authors = [
    {name = "the verible-python authors", email = "dev@dau.dev"},
]
description = "Python wrapping/binding for verible"
readme = "README.md"
license = { text = "Apache-2.0" }
<<<<<<< before updating
version = "0.0.4071.0"
=======
version = "0.1.0"
>>>>>>> after updating
requires-python = ">=3.10"
keywords = []

classifiers = [
    "Development Status :: 3 - Alpha",
    "Programming Language :: Python",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.10",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
    "Programming Language :: Python :: 3.13",
    "Programming Language :: Python :: 3.14",
]

dependencies = []

[project.optional-dependencies]
cli = [
    "rich",
    "typer",
]
develop = [
    "build",
    "bump-my-version",
    "check-dist",
<<<<<<< before updating
    "cibuildwheel",
=======
>>>>>>> after updating
    "codespell",
    "hatchling",
    "mdformat",
    "mdformat-tables>=1",
    "pytest",
    "pytest-cov",
    "ruff",
    "twine",
    "ty",
    "uv",
    "wheel",
]

[project.scripts]
verible-cli = "verible.cli:main"

[project.urls]
Repository = "https://github.com/dau-dev/verible-python"
Homepage = "https://github.com/dau-dev/verible-python"

[tool.bumpversion]
current_version = "0.0.4071.0"
commit = true
tag = true
commit_args = "-s"

[[tool.bumpversion.files]]
filename = "verible/__init__.py"
search = '__version__ = "{current_version}"'
replace = '__version__ = "{new_version}"'

[[tool.bumpversion.files]]
filename = "pyproject.toml"
search = 'version = "{current_version}"'
replace = 'version = "{new_version}"'

<<<<<<< before updating
[tool.cibuildwheel]
build = "cp310-* cp311-* cp312-* cp313-* cp314-*"
test-command = "echo 'TODO'"
test-requires = []

[tool.cibuildwheel.linux]
before-all = """make dependencies-linux && PATH=/usr/lib/ccache:/usr/local/opt/ccache/libexec:$PATH make build-verible && make copy-verible"""
repair-wheel-command = ""
skip = "*i686 musllinux*"

[tool.cibuildwheel.macos]
before-all = """make dependencies-macos && PATH=/usr/lib/ccache:/usr/local/opt/ccache/libexec:$PATH make build-verible && make copy-verible"""
repair-wheel-command = ""
environment = {MACOSX_DEPLOYMENT_TARGET="11.0"}
archs = "arm64"

[tool.cibuildwheel.windows]
before-all = """make dependencies-win && make build-verible && make copy-verible"""
archs = "AMD64"
skip = "*win32 *arm_64"

=======
>>>>>>> after updating
[tool.coverage.run]
branch = true
omit = [
    "verible/tests/integration/",
]

[tool.coverage.report]
exclude_also = [
    "raise NotImplementedError",
    "if __name__ == .__main__.:",
    "@(abc\\.)?abstractmethod",
]
ignore_errors = true
fail_under = 50

[tool.hatch.build]
artifacts = [
    "verible/bin/",
]
[tool.hatch.build.sources]
src = "/"

[tool.hatch.build.targets.sdist]
packages = [
    "verible",
<<<<<<< before updating
    "hatch_build.py",
    "src",
]
include = [
    "LICENSE",
    "README.md",
]

[tool.hatch.build.targets.wheel.hooks.custom]
=======
]
>>>>>>> after updating

[tool.hatch.build.targets.wheel]
packages = [
    "verible",
]
<<<<<<< before updating
exclude = [
    "/src",
]
=======
>>>>>>> after updating

[tool.hatch.build.targets.wheel.shared-data]
"verible/bin" = "bin"

[tool.pytest.ini_options]
addopts = [
    "-vvv",
    "--junitxml=junit.xml",
]
testpaths = "verible/tests"

[tool.ruff]
line-length = 150

[tool.ruff.lint]
extend-select = [
    "I",
]

[tool.ruff.lint.isort]
combine-as-imports = true
default-section = "third-party"
known-first-party = [
    "verible",
]
section-order = [
    "future",
    "standard-library",
    "third-party",
    "first-party",
    "local-folder",
]

[tool.ruff.lint.per-file-ignores]
"__init__.py" = [
    "F401",
    "F403",
]
